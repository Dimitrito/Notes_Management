How to run API service:

Go to the backend folder
cd backend

Restore dependencies
dotnet restore

Run the project
dotnet run

After start, API will be available at
http://localhost:5000
Swagger UI: http://localhost:5000/swagger